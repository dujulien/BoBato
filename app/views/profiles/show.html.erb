<% if @profile.first_name %>
	<%= content_for :title, "Profil de #{@profile.first_name}"%>
<% else %>
	<%= content_for :title, "Profil" %>
<% end %>

<!------------------------ BANNER ----------------------------------------------------->
<%= render 'show_profile_banner' %>
<!------------------------ END BANNER ----------------------------------------------------->

<%= render :partial => "shared/errors" %>

<!------------------------ USER PROFILE ----------------------------------------------------->
<%= render 'show', profile: @profile %>
<!------------------------ END USER PROFILE ----------------------------------------------------->
