<h1>New Article</h1>

  <%= render 'form' %>

  <%= form_with model: @convoy, local: true  do |form| %>
    <%= form.text_area :content %><br><br>
    <%= form.file_field :pictures, multiple: true %><br>
    <%= form.submit %>
  <% end %>

<%= link_to 'Back', convoys_path %>
